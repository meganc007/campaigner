import 'package:flutter/material.dart';

class TerrainDetailPage extends StatelessWidget {
  final String uuid;
  const TerrainDetailPage({super.key, required this.uuid});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
