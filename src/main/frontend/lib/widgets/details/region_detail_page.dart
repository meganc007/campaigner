import 'package:flutter/material.dart';

class RegionDetailPage extends StatelessWidget {
  final String uuid;
  const RegionDetailPage({super.key, required this.uuid});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
