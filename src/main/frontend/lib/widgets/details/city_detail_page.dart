import 'package:flutter/material.dart';

class CityDetailPage extends StatelessWidget {
  final String uuid;
  const CityDetailPage({super.key, required this.uuid});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
