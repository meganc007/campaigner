import 'package:flutter/material.dart';

class PlaceTypeDetailPage extends StatelessWidget {
  final String uuid;
  const PlaceTypeDetailPage({super.key, required this.uuid});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
