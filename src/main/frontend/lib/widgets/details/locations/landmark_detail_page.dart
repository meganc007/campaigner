import 'package:flutter/material.dart';

class LandmarkDetailPage extends StatelessWidget {
  final String uuid;
  const LandmarkDetailPage({super.key, required this.uuid});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
