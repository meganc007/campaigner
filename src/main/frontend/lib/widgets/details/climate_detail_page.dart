import 'package:flutter/material.dart';

class ClimateDetailPage extends StatelessWidget {
  final String uuid;
  const ClimateDetailPage({super.key, required this.uuid});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
