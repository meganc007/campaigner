import 'package:flutter/material.dart';

class AddCampaignPage extends StatelessWidget {
  const AddCampaignPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Create New Campaign")),
      body: Placeholder(child: Text("Under construction")),
    );
  }
}
